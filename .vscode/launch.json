{
    "configurations": [
        {
            "type": "pwa-chrome",
            "name": "flexvue",
            "request": "launch",
            "url": "http://localhost:8080",
            "webRoot": "${workspaceFolder}"
        }
    ]
}